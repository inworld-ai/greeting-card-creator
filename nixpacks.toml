[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["cd server && npm ci --include=dev"]

[phases.build]
cmds = ["echo 'Server is TypeScript - using ts-node at runtime'"]

[start]
cmd = "cd server && npm start"

