[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm ci --production=false"]

[phases.build]
cmds = ["echo 'Skipping frontend build - backend only'"]

[start]
cmd = "node server.js"

